{
  "_about": "Owner rulings behind the Wiring chapter. A ruling keeps the question it answered.",
  "rulings": [
    {
      "date": "2026-09-24",
      "issue": "#847",
      "question": "debug_print over a map_-shaped TSD[str, REF[TS[int]]] printed reference tokens and missed value ticks. Should a generic parameter ever bind a reference?",
      "ruling": "WHENEVER resolving a generic, we de-reference everything. If the code depends on REF it must express that.",
      "rules": [
        "WIR-7",
        "WIR-8",
        "WIR-9"
      ]
    },
    {
      "date": "2026-09-24",
      "issue": "#847",
      "question": "Should getitem_ work on references rather than values?",
      "ruling": "Be explicit and always deal with ref.",
      "rules": [
        "WIR-13"
      ],
      "note": "Applied to the structural projections (TSB getitem_ / getattr_). Selection by a key known only at run time was settled 2026-09-26 (below)."
    },
    {
      "date": "2026-09-26",
      "issue": null,
      "question": "Where do the wiring rules live, so that HGL and the runtime keep in line?",
      "ruling": "Extend the runtime specification with the wiring rules, following the same pattern as the rest of the runtime.",
      "rules": [
        "WIR-1",
        "WIR-14"
      ]
    },
    {
      "date": "2026-09-26",
      "issue": null,
      "question": "Does a bundle's name take part in matching?",
      "ruling": "Bundle names are only considered if both items are named, otherwise it is a property name and type match only; this supports named and unnamed bundles matching. If both are named, then the name is relevant.",
      "rules": [
        "WIR-15"
      ]
    },
    {
      "date": "2026-09-26",
      "issue": null,
      "question": "Should requesting REF[X] for an output whose pattern is a bundle fail?",
      "ruling": "REF[output_type] is perfectly reasonable as an output type.",
      "rules": [
        "WIR-12"
      ]
    },
    {
      "date": "2026-09-26",
      "issue": null,
      "question": "Should getitem_ with a key known only at run time take references explicitly?",
      "ruling": "It must still have a typed output once wired. The input is not specified as REF, so the value is dereferenced before wiring in; if the output is REF[...], the node requests a reference for the value and returns that. A mismatch against the expected output is a runtime error.",
      "rules": [
        "WIR-7",
        "WIR-12"
      ]
    },
    {
      "date": "2026-09-26",
      "issue": null,
      "question": "Does HGL's map<K, ref<V>> carry an outer reference?",
      "ruling": "map<K, ref<V>> gets a map containing ref values.",
      "rules": [
        "WIR-14"
      ],
      "note": "TSD[K, REF[V]]; HGL's type-extensions.md recorded an outer REF, which is withdrawn."
    }
  ]
}
