# Read the Docs installs this file to build the HTML documentation.
#
# The HTML build deliberately does not import hgraph: no page uses an autodoc
# directive, and mermaid renders client-side. Keep it that way — it means the
# docs build needs no C++ compiler and stays fast.
#
# The doctest build (`sphinx-build -b doctest`) DOES need an importable hgraph;
# that runs in CI against a built wheel, not here. See .github/workflows/docs.yml.
#
# Keep in sync with the `docs` extra in pyproject.toml.
sphinx>=7.4
sphinx-rtd-theme>=2.0
myst-parser>=2.0
sphinxcontrib-mermaid>=1.0
sphinxcontrib-bibtex>=2.6
pybtex-apa-style>=1.3
