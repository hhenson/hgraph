# HGL implementation catalogue

Generated by `python tools/hgl_catalogue.py`; edit `status.json` to record a review.

Compiled HGL bodies and bindings to HGL-exposed native value functions count as completed authoring work. A temporal-node delegation stays pending. Core replacement is deferred.
Implementation form and remaining overload domains are recorded separately; an implemented slice is not a claim of complete family parity.

**210 core operator identities**, 134 blocked, 26 implemented, 44 implemented-slice, 6 native-provider.

The [machine-readable catalogue](catalogue.json) joins declarations, registration sites, expanded registry signatures, HGL sources, and review decisions.
Source registration templates are included without pretending that each site is one concrete candidate. Optional extension declarations and registrations have separate records.

## Priority order

1. Native error/ownership bindings and lifecycle/startup/activation (B1, B2).
2. Open scalar/schema and structural/reference parity (B3, B4).
3. Domain providers and higher-order lifecycle (B5, B6).

Implemented means the recorded domain is authored and tested; it does not promise production API replacement or every generic specialization.

## Operators

| Operator | Review status | HGL forms | Remaining work / evidence |
| --- | --- | --- | --- |
| `__apply_value_callable` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `__assert_fmt` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `__call_value_callable` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `__json_array` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `__json_object` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `__lag_proxy` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `__log_sink` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `__materialize` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `__print_sink` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `__recovering_pass_through` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `__tick_count` | implemented | hgl-runtime | Internal signal tick counter implemented as hgraph.std.tick_count; prior output is the counter.  |
| `abs_` | implemented-slice | hgl-runtime | i64 and f64 unary bodies. Remaining: checked duration/period overloads. Itemwise TSL/TSB candidates also remain. [B1](#b1), [B4](#b4) |
| `add_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `all_` | blocked | contract only | Existing HGL variadic contract only. Native binary and collection candidates schedule an initial result. [B2](#b2) |
| `and_` | implemented-slice | hgl-runtime | Closed bool/i64/f64/str truthiness and native mixed numeric binary domains. Remaining: atomic containers and arbitrary registered scalar truthiness. [B3](#b3) |
| `any_` | blocked | contract only | Existing HGL variadic contract only. Native binary and collection candidates schedule an initial result. [B2](#b2) |
| `apply` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `assert_` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `at_zone` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `batch` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `bit_and` | implemented-slice | hgl-runtime | Native bool/i64 scalar bit projections in HGL bodies. Remaining: set/itemwise overload domains where registered. [B3](#b3), [B4](#b4) |
| `bit_or` | implemented-slice | hgl-runtime | Native bool/i64 scalar bit projections in HGL bodies. Remaining: set/itemwise overload domains where registered. [B3](#b3), [B4](#b4) |
| `bit_xor` | implemented-slice | hgl-runtime | Native bool/i64 scalar bit projections in HGL bodies. Remaining: set/itemwise overload domains where registered. [B3](#b3), [B4](#b4) |
| `call` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `cast_` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `cmp_` | blocked | — | CmpResult/enum/open scalar or structural comparison domains. [B3](#b3) |
| `collapse_keys` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `collect` | implemented-slice | hgl-runtime | Scalar key/value accumulation into TSD for all eight emitted scalar types. Remaining: TSS duplicate adds must publish empty deltas (HGL upsert suppresses them), map reset/exclusion, owned containers and arbitrary structural values. [B3](#b3), [B4](#b4) |
| `combine` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `combine_cs` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `combine_json` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `combine_map` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `combine_tsd` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `combine_tss_from_tsl` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `compare` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `concat` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `const` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `contains_` | implemented-slice | hgl-runtime | String byte membership; set membership/subsets and map keys for all eight emitted scalar types; map values retain unread resolver slots. Remaining: open registered scalar and atomic-container variants. Reference-forwarding boundness remains in the structural parity domain. [B3](#b3), [B4](#b4) |
| `convert` | implemented-slice | hgl-runtime | Six bool/i64/f64 cross-conversions and Date/DateTime projections via native value functions; named to_int/to_float/to_bool/to_date/to_datetime. Remaining: general type-directed overloads and owned/container conversions. [B3](#b3), [B4](#b4), [B5](#b5) |
| `convert_zone` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `datepart` | implemented | hgl-runtime | DateTime epoch seconds and midnight DateTime.  |
| `day` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `day_of_month` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `days` | implemented | hgl-runtime | Duration components, including normalized negative values.  |
| `debug_print` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `dedup` | implemented-slice | hgl-runtime | Exact bool/i64/str/date/time/datetime/duration; f64 default tolerance and explicit temporal tolerance alias. Remaining: structural/arbitrary scalar state. Temporal defaults cannot yet be declared on an HGL contract. [B3](#b3), [B4](#b4) |
| `default` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `dereference` | blocked | — | Generic structural/reference field and item access semantics. [B4](#b4) |
| `difference` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `dispatch_` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `div_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `divmod_` | blocked | — | Integer result needs fixed-list indexed output writes; floating result additionally needs the coupled throwing quotient/remainder value kernel. [B1](#b1), [B4](#b4) |
| `downcast_` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `downcast_ref` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `drop` | implemented-slice | hgl-runtime | bool/i64/f64/str/date/time/datetime/duration scalar forwarding bodies. Remaining: open value, structural delta and reference domains. [B3](#b3), [B4](#b4) |
| `emit` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `eq_` | implemented-slice | hgl-runtime | bool/i64/str/date/datetime/duration and mixed numeric equality; floating tolerance, including explicit const epsilon alias. Remaining: arbitrary registered scalars and structural domains. [B3](#b3), [B4](#b4) |
| `evaluation_time_in_range` | blocked | — | Startup/scheduler or wall-clock capability. [B2](#b2) |
| `explode` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `filter_` | implemented-slice | hgl-runtime | bool/i64/f64/str/date/time/datetime/duration scalar forwarding bodies. Remaining: open value, structural delta and reference domains. Remaining: unchecked activation on input invalidation is not expressible without an explicit empty validity set. [B2](#b2), [B3](#b3), [B4](#b4) |
| `filter_by` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `filter_cs` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `filter_frame` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `filter_tsd_by_matches` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `flip` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `flip_keys` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `floordiv_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `format_` | blocked | — | Owned string/sequence results, formatting/regex dependencies and errors. [B1](#b1) |
| `freeze` | implemented-slice | hgl-runtime | Eight scalar domains: forward until the predicate is true, then passivate both inputs (ADR 0010). Remaining: structural delta forwarding and the callable-predicate compositions. [B4](#b4), [B6](#b6) |
| `from_data_frame` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `from_data_frame_batches` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `from_json` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `from_table` | blocked | — | TABLE protocol, row schemas and record/replay ownership. [B5](#b5) |
| `from_table_const` | blocked | — | TABLE protocol, row schemas and record/replay ownership. [B5](#b5) |
| `gate` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `ge_` | implemented-slice | hgl-runtime | Materialized numeric, string and legacy temporal scalar comparisons; ne_ also bool. Remaining: enum/arbitrary scalar and structural candidates. [B3](#b3), [B4](#b4) |
| `getattr_` | blocked | — | Generic structural/reference field and item access semantics. [B4](#b4) |
| `getitem_` | blocked | — | Generic structural/reference field and item access semantics. [B4](#b4) |
| `group_by` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `gt_` | implemented-slice | hgl-runtime | Materialized numeric, string and legacy temporal scalar comparisons; ne_ also bool. Remaining: enum/arbitrary scalar and structural candidates. [B3](#b3), [B4](#b4) |
| `hour` | implemented | hgl-runtime | Time/DateTime clock fields.  |
| `if_` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `if_cmp` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `if_then_else` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `if_true` | implemented | hgl-runtime | Boolean ticks and const tick_once_only default false.  |
| `index_of` | implemented-slice | hgl-runtime | Fixed-list i64/f64/str/bool/date/time/datetime/duration; retained size, skip invalid children, suppress unchanged index. Remaining: arbitrary registered scalar equality. [B3](#b3) |
| `intersection` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `invert_` | implemented-slice | hgl-runtime | Native bool/i64 scalar bit projections in HGL bodies. Remaining: set/itemwise overload domains where registered. [B3](#b3), [B4](#b4) |
| `is_empty` | implemented-slice | hgl-runtime | Previously implemented str, fixed/unbounded list, set and map query bodies. Remaining: collection startup parity, rolling materialization and TSB schema domains. [B2](#b2), [B3](#b3), [B4](#b4) |
| `isoformat` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `isoweekday` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `join` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `json_as_bool` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `json_as_float` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `json_as_int` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `json_as_str` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `json_decode` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `json_encode` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `keys_` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `lag` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `last_modified_date` | implemented-slice | hgl-runtime | Signal endpoint modification timestamp/calendar date. Remaining: unchecked activation on input invalidation is not expressible without an explicit empty validity set. [B2](#b2) |
| `last_modified_time` | implemented-slice | hgl-runtime | Signal endpoint modification timestamp/calendar date. Remaining: unchecked activation on input invalidation is not expressible without an explicit empty validity set. [B2](#b2) |
| `last_modified_wall_clock_time` | blocked | — | Startup/scheduler or wall-clock capability. [B2](#b2) |
| `le_` | implemented-slice | hgl-runtime | Materialized numeric, string and legacy temporal scalar comparisons; ne_ also bool. Remaining: enum/arbitrary scalar and structural candidates. [B3](#b3), [B4](#b4) |
| `len_` | implemented-slice | hgl-runtime | Previously implemented str, fixed/unbounded list, set and map query bodies. Remaining: collection startup parity, rolling materialization and TSB schema domains. [B2](#b2), [B3](#b3), [B4](#b4) |
| `ln` | implemented | hgl-runtime | f64 natural logarithm (native value binding).  |
| `log_` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `lshift_` | implemented | hgl-runtime | i64 body through the checked `shift_left` native binding; negative and too-large counts raise the native kernel's exception (ADR 0009). Itemwise TSL/TSB candidates remain (B4). [B4](#b4) |
| `lt_` | implemented-slice | hgl-runtime | Materialized numeric, string and legacy temporal scalar comparisons; ne_ also bool. Remaining: enum/arbitrary scalar and structural candidates. [B3](#b3), [B4](#b4) |
| `make_tsd` | implemented-slice | hgl-runtime | Scalar key/value domains across the eight emitted types, with explicit remove-key variant. Remaining: generic structural child delta/value transport. [B3](#b3), [B4](#b4) |
| `map_` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `match_` | blocked | — | Owned string/sequence results, formatting/regex dependencies and errors. [B1](#b1) |
| `max_` | implemented-slice | hgl-runtime | Binary i64/f64/str/date/datetime/duration plus mixed numeric; unary running extrema for same-type domains. Remaining: non-strict/default, aggregate startup, enum and arbitrary scalar domains. Running bool is also implemented. [B2](#b2), [B3](#b3), [B4](#b4) |
| `max_ts_list` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `mean` | implemented-slice | hgl-runtime | Running i64/f64 and binary mixed numeric mean. Remaining: collection/variadic aggregate domains. [B2](#b2), [B3](#b3), [B4](#b4) |
| `merge` | blocked | contract only | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `merge_tsd_disjoint` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `mesh_` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `microsecond` | implemented | hgl-runtime | Time/DateTime clock fields.  |
| `microseconds` | implemented | hgl-runtime | Duration components, including normalized negative values.  |
| `min_` | implemented-slice | hgl-runtime | Binary i64/f64/str/date/datetime/duration plus mixed numeric; unary running extrema for same-type domains. Remaining: non-strict/default, aggregate startup, enum and arbitrary scalar domains. Running bool is also implemented. [B2](#b2), [B3](#b3), [B4](#b4) |
| `min_ts_list` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `minute` | implemented | hgl-runtime | Time/DateTime clock fields.  |
| `mod_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `modified` | blocked | — | Startup/scheduler or wall-clock capability. [B2](#b2) |
| `month` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `month_of_year` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `mul_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `ne_` | implemented-slice | hgl-runtime | Materialized numeric, string and legacy temporal scalar comparisons; ne_ also bool. Remaining: enum/arbitrary scalar and structural candidates. [B3](#b3), [B4](#b4) |
| `neg_` | implemented-slice | hgl-runtime | i64 and f64 unary bodies. Remaining: checked duration/period overloads. Itemwise TSL/TSB candidates also remain. [B1](#b1), [B4](#b4) |
| `not_` | implemented-slice | hgl-runtime | Closed bool/i64/f64/str truthiness and native mixed numeric binary domains. Remaining: atomic containers and arbitrary registered scalar truthiness. [B3](#b3) |
| `nothing` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `null_sink` | implemented | hgl-runtime | Signal input-view sink: all endpoint shapes without reading the payload.  |
| `or_` | implemented-slice | hgl-runtime | Closed bool/i64/f64/str truthiness and native mixed numeric binary domains. Remaining: atomic containers and arbitrary registered scalar truthiness. [B3](#b3) |
| `partition` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `pos_` | implemented-slice | hgl-runtime | i64, f64 and duration identity bodies. Remaining: itemwise TSL/TSB candidates and their child-delta policies. [B4](#b4) |
| `pow_` | implemented-slice | hgl-runtime | i64/f64 and mixed numeric bodies through the checked `power` native binding; the native exceptions (negative integer exponent, overflow, zero to a negative power) are raised identically (ADR 0009). Remaining: the `divide_by_zero` policy parameter (enum, B3) and itemwise TSL/TSB candidates (B4). [B3](#b3), [B4](#b4) |
| `print_` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `race` | blocked | contract only | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `range_adjacent` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_contains` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_difference` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_extent` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_hull` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_intersection` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_merge` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_mergeable` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_overlaps` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_shift` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_touches` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `range_union` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `record` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `reduce` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `reduce_tsd_of_bundles_with_race` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `reduce_tsd_with_race` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `rekey` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `replace` | blocked | — | Owned string/sequence results, formatting/regex dependencies and errors. [B1](#b1) |
| `replay` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `replay_const` | native-provider | — | Runtime source/materialization or recovery provider. HGL surface needs startup and provider lifecycle; native ownership remains appropriate. [B2](#b2), [B5](#b5) |
| `replay_data_frame` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `request_id` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `resolve_civil` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `round_` | implemented | hgl-runtime | f64 with temporal i64 decimal places, native formatting-round algorithm.  |
| `route_by_index` | blocked | — | Reference selection/reselection or structural merge/race semantics. [B4](#b4) |
| `rshift_` | implemented | hgl-runtime | i64 body through the checked `shift_right` native binding; negative and too-large counts raise the native kernel's exception (ADR 0009). Itemwise TSL/TSB candidates remain (B4). [B4](#b4) |
| `sample` | implemented-slice | hgl-runtime | bool/i64/f64/str/date/time/datetime/duration scalar forwarding bodies. Remaining: open value, structural delta and reference domains. [B3](#b3), [B4](#b4) |
| `schedule` | implemented-slice | hgl-runtime | Constant-delay source through `inject scheduler`, `start { scheduler.schedule(...) }`, `when scheduled()` and a `cache` tick counter that stays outside record/replay like the native State<Int> (ADR 0010/0011); initial_delay, max_ticks and use_wall_clock covered. Remaining: the time-series delay and start-time overloads (clock arithmetic in a runtime body) and the positive-delay validation in start. [B3](#b3) |
| `second` | implemented | hgl-runtime | Time/DateTime clock fields.  |
| `seconds` | implemented | hgl-runtime | Duration components, including normalized negative values.  |
| `setattr_` | blocked | — | Generic structural/reference field and item access semantics. [B4](#b4) |
| `sign` | implemented | hgl-runtime | i64/f64, including native sign(0)=1 and NaN policy.  |
| `slice_` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `sorted_` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `split` | blocked | — | Owned string/sequence results, formatting/regex dependencies and errors. [B1](#b1) |
| `step` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `stop_engine` | blocked | — | Callable, logging/formatting, recording or engine-control capability. [B5](#b5) |
| `str_` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `sub_` | implemented-slice | hgl-runtime | Numeric i64/f64 pairs (including mixed inputs); add_ also str/str. Remaining: checked temporal and structural/open scalar overloads. [B1](#b1), [B3](#b3), [B4](#b4) |
| `substr` | implemented | hgl-runtime | Live str/i64/i64 slice through the `slice` native value binding with Python slice normalisation. The parallel identity names the positions `begin`/`end` because `start` is an HGL reserved word.  |
| `sum_` | implemented-slice | hgl-runtime | Running i64/f64, including reset on any Boolean tick; parallel names sum and sum_reset. Remaining: collection/variadic aggregates and their initial scheduling. [B2](#b2), [B3](#b3), [B4](#b4) |
| `switch_` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `symmetric_difference` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `take` | implemented-slice | hgl-runtime | Eight scalar domains with `passivate` once the count is reached (ADR 0010). Remaining: structural delta forwarding, the reset and by-time overloads, and passivating a restored exhausted counter in `start` (a start block cannot reach the inputs). [B2](#b2), [B4](#b4) |
| `temporal_bucket` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `temporal_ceil` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `temporal_floor` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `temporal_round` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `throttle` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `timestamp` | implemented | hgl-runtime | DateTime epoch seconds and midnight DateTime.  |
| `to_civil` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `to_data_frame` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `to_instant` | blocked | — | Temporal range, civil/zone policy or owned formatting native bindings. [B5](#b5) |
| `to_json` | blocked | — | JSON value/type and codec native bindings. [B5](#b5) |
| `to_table` | blocked | — | TABLE protocol, row schemas and record/replay ownership. [B5](#b5) |
| `to_window` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `total_seconds` | implemented | hgl-runtime | Duration components, including normalized negative values.  |
| `try_except` | blocked | — | Dynamic callable and nested graph ownership contracts. [B6](#b6) |
| `type_` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |
| `uncollapse_keys` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `ungroup` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `union` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `unpartition` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `until_true` | implemented-slice | hgl-runtime | Boolean body that passivates its input once true (ADR 0010). Remaining: the value-callable and wired-function predicate overloads. [B6](#b6) |
| `valid` | blocked | — | Startup/scheduler or wall-clock capability. [B2](#b2) |
| `values_` | blocked | — | Generic keyed/structural transformations, aggregate startup and delta policies. [B4](#b4) |
| `weekday` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `window` | blocked | — | Scheduling, passivation, buffered deltas or callable lifecycle. [B2](#b2) |
| `with_columns` | blocked | — | Frame/series native types, schema policies and Arrow-backed algorithms. [B5](#b5) |
| `year` | implemented | hgl-runtime | Date/DateTime projections with native date-only no-change suppression.  |
| `zero` | blocked | — | Type-directed output, arbitrary scalar/compound conversion and lifecycle semantics. [B3](#b3) |

## Blocker definitions

### B1

Priority 1. Native errors and owned results: the exception half is admitted (ADR 0009: `throws` natives, descriptor policy `translated`, a raise ends the evaluation under hgraph's node error model) and scalar results including `str` are returned by value. Still missing: owned sequence/collection results (`split`), regex and formatting kernels that carry compiled state or packs (`match_`, `replace`, `format_`), and the coupled quotient/remainder result of `divmod_`.

Evidence: [0009-native-errors-and-the-node-error-model.md](../../../language/docs/design/decisions/0009-native-errors-and-the-node-error-model.md), [native-interface.md](../../../language/docs/design/native-interface.md), [string_impl.h](../../../include/hgraph/lib/std/operators/impl/string_impl.h)

### B2

Priority 1. Lifecycle and activation: the clock and scheduler injectables, the scheduled() handler selector, passivate/activate (ADR 0010) and a single scalar cache outside record/replay (ADR 0011) are admitted. Still missing: several cache fields, cache beside recordable state (native static nodes reject State with RecordableState), buffered delta queues and window state for throttle/batch/gate/lag/window (generic non-scalar state, MIG-005), passivation from a start block, an explicitly empty validity set for bound-but-invalid inputs (LIB-002), and external resource ownership.

Evidence: [0010-lifecycle-capabilities.md](../../../language/docs/design/decisions/0010-lifecycle-capabilities.md), [0011-cache-declarations.md](../../../language/docs/design/decisions/0011-cache-declarations.md), [stream_impl.h](../../../include/hgraph/lib/std/operators/impl/stream_impl.h)

### B3

Priority 2. Open value/schema domains: runtime generic value operations, type carriers, arbitrary registered scalar/enum/Any and compound atomic schemas need native type mappings and residual generic constraints. Concrete admitted scalar slices are catalogued separately. CivilDateTime/ZonedDateTime/TimeZone value emission currently fails closed in cpp_emitter.cpp; ZonedTime is also unimplemented.

Evidence: [roadmap.md](../../../language/docs/design/roadmap.md), [native-surface-proposal.md](../../../language/docs/design/native-surface-proposal.md), [cpp_emitter.cpp](../../../language/src/codegen/cpp_emitter.cpp)

### B4

Priority 2. Structural/reference parity: general delta transport, reference equality/reselection, recordable non-scalar state, output invalidation and shape-dependent policies are not yet covered by the admitted HGL implementation. Existing structural queries and bounded membership/index slices are credited separately.

Evidence: [roadmap.md](../../../language/docs/design/roadmap.md), [language-model.md](../../../language/docs/design/language-model.md), [collection_input_semantics.h](../../../include/hgraph/lib/std/operators/impl/collection_input_semantics.h)

### B5

Priority 3. Native-backed domain types and resources: JSON trees, table/frame/series metadata, callable providers, range/period/zone policies, I/O and callbacks need public HGL native type/lifecycle bindings. Native providers remain the implementation owners; thin temporal delegations do not complete authoring.

Evidence: [native-interface.md](../../../language/docs/design/native-interface.md), [roadmap.md](../../../language/docs/design/roadmap.md)

### B6

Priority 3. Higher-order authoring contracts: generic/variadic callable metadata, nested graph/service lifecycle and dynamic signatures need admitted HGL mappings before complete core graph families can be authored. Existing compiler lowering to runtime graph operators is substrate, not a rewritten library body.

Evidence: [roadmap.md](../../../language/docs/design/roadmap.md), [higher_order_impl.h](../../../include/hgraph/lib/std/operators/impl/higher_order_impl.h)

## Other library surfaces

| Surface | Disposition | Reason |
| --- | --- | --- |
| `pass_through_node` | implemented-slice | Eight scalar domains implemented as hgraph.std.pass_through; general structural delta forwarding remains B4. |
| `components/services/adaptors` | native-provider | Runtime/native provider ownership and external lifecycle; B5/B6. |
| `optional extension library nodes` | inventory-only | Optional extension declarations and registration sites are separate records; extension candidate-domain reviews are outside this core standard-library pass. |

## Maintenance

```sh
python tools/hgl_catalogue.py --check
# After changing native library registrations, use a freshly built wheel:
python tools/hgl_catalogue.py --refresh-registry
```

See [the design record](../../docs/design/migration-catalogue.md) and the
[requirements ledger](../../docs/design/migration-requirements.md) for the `HGL-MIG-*` and `HGL-LIB-*` identifiers.
