// Agreed design example; existing-binding forwarding is not yet supported by
// the compiler. See language/docs/design/control-flow.md.

fn use_existing_conditional_result(condition: bool, x: i64) -> i64 {
    var r: i64 = x
    if condition {
        r = r + 1
    }

    return r * 2
}
